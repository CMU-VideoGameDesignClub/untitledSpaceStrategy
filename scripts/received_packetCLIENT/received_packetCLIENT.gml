/*/ might need to throw in function loop as of 2.3
buffer = argument0;
msgid = buffer_read(buffer, buffer_u8);

/*
switch(msgid)
{
	
}

/*

need to throw server on separate project on home pc and port forwarded to 64198

*/